[project]
name = "lima"
version = "0.1.0"
description = "LIMA from (mostly) scratch"
requires-python = ">=3.10"
# Runtime dependencies live in requirements.in / requirements.txt, not here.
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["lima"]
